# Cora Data

Place the competition release file here:

```text
warmup/cora_gcn/data/cora.pkl
```

The dataset file is intentionally not tracked in Git. It should be downloaded
from the competition release or copied from the official warm-up package before
running `gcn.py`.
